[package]
name = "dagrid-plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
nih_plug = { git = "https://github.com/robbert-vdh/nih-plug", version = "0.0.0", features = [
    "simd",
] }
dagrid-core = { path = "../core" }
